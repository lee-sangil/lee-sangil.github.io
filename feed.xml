---
layout: null
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ '/feed.xml' | absolute_url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" />
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ '/feed.xml' | absolute_url }}</id>
  <title type="html">{{ site.title | xml_escape }}</title>
  {% if site.description %}<subtitle>{{ site.description | xml_escape }}</subtitle>{% endif %}
  {% if site.author.name %}<author><name>{{ site.author.name | xml_escape }}</name></author>{% endif %}

  {% comment %}
    Merge site.posts (English) and site.ko_posts (Korean),
    sort by date descending, limit to 20 entries.
  {% endcomment %}
  {% assign all_posts = site.posts | concat: site.ko_posts | sort: "date" | reverse %}
  {% for post in all_posts limit:20 %}
  <entry{% if post.lang == "ko" %} xml:lang="ko"{% endif %}>
    <title type="html">{{ post.title | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}" />
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ post.id | absolute_url }}</id>
    <content type="html" xml:base="{{ post.url | absolute_url }}">{{ post.content | xml_escape }}    {% if post.author %}<author><name>{{ post.author | xml_escape }}</name></author>{% endif %} </content>
    {% if post.excerpt %}<summary type="html">{{ post.excerpt | strip_html | xml_escape }}</summary>{% endif %}
    {% for tag in post.tags %}<category term="{{ tag | xml_escape }}" />{% endfor %}
    {% for cat in post.categories %}<category term="{{ cat | xml_escape }}" />{% endfor %}
  </entry>
  {% endfor %}
</feed>
